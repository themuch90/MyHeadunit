name: headunit_app
description: Interfaccia head unit (flutter-pi)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  video_player: ^2.14.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
